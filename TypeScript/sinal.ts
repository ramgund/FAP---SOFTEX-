function verificarSinal(x:number) {
    if (x > 0) {
        console.log("Positivo")
    } else if (x < 0) {
        console.log("Negativo")
    } else if (x == 0) {
        console.log("Neutro")
    }
}

verificarSinal(0)